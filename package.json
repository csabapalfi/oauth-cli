{
  "name": "local-oauth",
  "version": "0.1.0",
  "description": "Play around with OAuth2 APIs locally",
  "license": "MIT",
  "scripts": {
    "test": "mocha --require ./test/setup.js"
  },
  "dependencies": {
    "opener": "^1.4.3",
    "request": "^2.81.0"
  },
  "devDependencies": {
    "chai": "^3.5.0",
    "mocha": "^3.3.0",
    "proxyquire": "^1.7.11"
  }
}
